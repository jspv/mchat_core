[pytest]
markers =
    tools: tests that require optional tool packages or APIs
    live_llm: integration tests that call live LLMs (require OPENAI_API_KEY)
asyncio_mode = auto
addopts = -m "not live_llm"
